<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.boom.rbac.mapper.PermissionMapper" >
    <resultMap id="BaseResultMap" type="Permission" >
        <id column="id" property="id" jdbcType="VARCHAR" />
        <result column="name" property="name" jdbcType="VARCHAR" />
        <result column="type" property="type" jdbcType="VARCHAR" />
        <result column="url" property="url" jdbcType="VARCHAR" />
        <result column="permission" property="permission" jdbcType="VARCHAR" />
        <result column="parent_id" property="parentId" jdbcType="VARCHAR" />
        <result column="parent_ids" property="parentIds" jdbcType="VARCHAR" />
        <result column="available" property="available" jdbcType="SMALLINT" />
    </resultMap>
    <sql id="permissionFields">
        id,name,type,url,permission,parent_id,parent_ids,available
    </sql>

    <select id="queryAll" resultMap="BaseResultMap">
        SELECT
        <include refid="permissionFields"/>
        FROM
        permission
    </select>

    <select id="queryById" resultMap="BaseResultMap">
        SELECT
        <include refid="permissionFields"/>
        FROM
        permission
        WHERE
        id = #{permissionId}
    </select>

    <select id="queryByIds" resultMap="BaseResultMap">
        SELECT
        <include refid="permissionFields"/>
        FROM
        permission
        WHERE
        id IN
        <foreach collection="permissionIds" item="permissionId" index="index" open="(" separator="," close=")">
            #{permissionId}
        </foreach>
    </select>

    <select id="queryChildren" resultMap="BaseResultMap">
        SELECT
        <include refid="permissionFields"/>
        FROM
        permission
        WHERE
        parent_id = #{permissionId}
    </select>

    <select id="queryAllChildren" resultMap="BaseResultMap">
        SELECT
        <include refid="permissionFields"/>
        FROM
        permission
        WHERE
        parent_id IN
        <foreach collection="permissionIds" index="index" item="permissionId" open="(" separator="," close=")">
            #{permissionId}
        </foreach>
    </select>

    <select id="queryLikeName" resultMap="BaseResultMap">
        SELECT
        <include refid="permissionFields"/>
        FROM
        permission
        WHERE
        name LIKE concat('%',#{name},'%')
    </select>

    <delete id="deleteById">
        DELETE FROM permission WHERE id = #{permissionId}
    </delete>

    <update id="updatePermission">
        UPDATE permission
        <set>
            <if test="null != name">name = #{name},</if>
            <if test="null != type">type = #{type},</if>
            <if test="null != url">url = #{url},</if>
            <if test="null != permission">permission = #{permission},</if>
            <if test="null != parent_id">parent_id = #{parentId},</if>
            <if test="null != parent_ids">parent_ids = #{parentIds}</if>
        </set>
        WHERE
        id = #{id}
    </update>

    <insert id="createPermission">
        INSERT INTO permission(id,name,type,url,permission,parent_id,parent_ids,available)
        VALUES (#{id},#{name},#{type},#{url},#{permission},#{parentId},#{parentIds},#{available})
    </insert>
</mapper>